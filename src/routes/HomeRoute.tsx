import { Component } from "solid-js"

interface HomeRouteProps {
}

export const HomeRoute: Component<HomeRouteProps> = () => {

  return (
    <div class="flex h-dvh w-full">
      <main>
        home page
      </main>
    </div>
  )
}

